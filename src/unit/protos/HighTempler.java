package unit.protos;

import unit.Unit;
import unit.Walkable;

public class HighTempler extends Unit implements Walkable {
    public HighTempler() {
        super(10, 2, "HighTempler", "Protos");
    }
}